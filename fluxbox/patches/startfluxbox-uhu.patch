diff -Nur orig/util/startfluxbox.in mod/util/startfluxbox.in
--- orig/util/startfluxbox.in	2018-09-30 09:03:17.000000000 +0200
+++ mod/util/startfluxbox.in	2018-09-30 13:37:35.409119695 +0200
@@ -23,6 +23,13 @@
     shift
 done
 
+# start pulseaudio
+if [ -f /etc/fluxbox-pulseaudio ]; then
+    if [ -x /usr/bin/pulseaudio ]; then
+      /usr/bin/pulseaudio -D
+    fi
+fi
+
 if [ -x "$startup" ]; then
     exec "$startup"
 elif [ -r "$startup" ]; then
@@ -30,6 +37,7 @@
 else
     if [ ! -d $fluxdir ]; then
         mkdir -p "$fluxdir/backgrounds" "$fluxdir/styles" "$fluxdir/pixmaps"
+        ln -sf /usr/share/backgrounds/UHU/*.jpg "$fluxdir/backgrounds/"
     fi
     if [ ! -r "$startup" ]; then
         ( cat << EOF
@@ -40,7 +48,13 @@
 # Lines starting with a '#' are ignored.
 
 # Change your keymap:
-xmodmap "$HOME/.Xmodmap"
+# xmodmap "$HOME/.Xmodmap"
+#
+# Start policykit agent
+lxpolkit &
+#
+# Random background
+fbsetbg -r $HOME/.fluxbox/backgrounds/
 
 # Applications you want to run with fluxbox.
 # MAKE SURE THAT APPS THAT KEEP RUNNING HAVE AN ''&'' AT THE END.
