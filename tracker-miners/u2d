dirs=$(urllist https://download.gnome.org/sources/tracker-miners/ | grep -E '^[0-9]\.[0-9]$' | sort -r)
for dir in $dirs;do
ver=$(u2d https://download.gnome.org/sources/tracker-miners/$dir)
if [ "$ver" ];then
echo $ver
exit 0
fi
done
