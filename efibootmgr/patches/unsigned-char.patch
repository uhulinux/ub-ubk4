diff -Naur a/src/efibootdump.c b/src/efibootdump.c
--- a/src/efibootdump.c	2018-06-10 22:12:10.000000000 +0200
+++ b/src/efibootdump.c	2022-05-11 23:53:20.748085587 +0200
@@ -69,7 +69,7 @@
 	text_path = alloca(text_path_len);
 	if (!text_path)
 		error(100, "Couldn't allocate memory");
-	rc = efidp_format_device_path(text_path, text_path_len,
+	rc = efidp_format_device_path((unsigned char *)text_path, text_path_len,
 				      dp, pathlen);
 	if (rc < 0) {
 		printf("<bad device path>");
diff -Naur a/src/efibootmgr.c b/src/efibootmgr.c
--- a/src/efibootmgr.c	2018-06-10 22:12:10.000000000 +0200
+++ b/src/efibootmgr.c	2022-05-11 23:53:05.160032179 +0200
@@ -949,7 +949,7 @@
 			pathlen = efi_loadopt_pathlen(load_option,
 						      boot->data_size);
 			dp = efi_loadopt_path(load_option, boot->data_size);
-			rc = efidp_format_device_path(text_path, text_path_len,
+			rc = efidp_format_device_path((unsigned char *)text_path, text_path_len,
 						      dp, pathlen);
 			if (rc < 0)
 				error(18, "Could not parse device path");
@@ -960,7 +960,7 @@
 			if (!text_path)
 				error(19, "Could not parse device path");
 
-			rc = efidp_format_device_path(text_path, text_path_len,
+			rc = efidp_format_device_path((unsigned char *)text_path, text_path_len,
 						      dp, pathlen);
 			if (rc < 0)
 				error(20, "Could not parse device path");
