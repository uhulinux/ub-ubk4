for dir in `urllist https://quantum-mirror.hu/mirrors/pub/gnu/unifont | grep ^unifont | sort -Vr`;do
ver=$(u2d https://quantum-mirror.hu/mirrors/pub/gnu/unifont/$dir unifont-)
if [ "$ver" ];then
echo $ver
exit 0
fi
done
