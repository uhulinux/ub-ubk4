diff -Naur orig/src/hb-common.cc patched/src/hb-common.cc
--- orig/src/hb-common.cc	2018-08-02 11:03:42.000000000 +0200
+++ patched/src/hb-common.cc	2018-08-13 02:18:53.344964845 +0200
@@ -641,6 +641,26 @@
   return HB_VERSION_ATLEAST (major, minor, micro);
 }
 
+/**
+ * hb_version_check:
+ * @major: 
+ * @minor: 
+ * @micro: 
+ *
+ * 
+ *
+ * Return value: 
+ *
+ * Since: 1.0
+ **/
+hb_bool_t
+hb_version_check (unsigned int major,
+		    unsigned int minor,
+		    unsigned int micro)
+{
+  return HB_VERSION_CHECK (major, minor, micro);
+}
+
 
 
 /* hb_feature_t and hb_variation_t */
diff -Naur orig/src/hb-version.h patched/src/hb-version.h
--- orig/src/hb-version.h	2018-08-09 07:01:38.000000000 +0200
+++ patched/src/hb-version.h	2018-08-13 02:15:23.534380748 +0200
@@ -45,6 +45,9 @@
 #define HB_VERSION_ATLEAST(major,minor,micro) \
 	((major)*10000+(minor)*100+(micro) <= \
 	 HB_VERSION_MAJOR*10000+HB_VERSION_MINOR*100+HB_VERSION_MICRO)
+#define HB_VERSION_CHECK(major,minor,micro) \
+	((major)*10000+(minor)*100+(micro) >= \
+	 HB_VERSION_MAJOR*10000+HB_VERSION_MINOR*100+HB_VERSION_MICRO)
 
 
 HB_EXTERN void
@@ -60,6 +63,11 @@
 		    unsigned int minor,
 		    unsigned int micro);
 
+hb_bool_t
+hb_version_check (unsigned int major,
+		    unsigned int minor,
+		    unsigned int micro);
+
 
 HB_END_DECLS
 
diff -Naur orig/src/hb-version.h.in patched/src/hb-version.h.in
--- orig/src/hb-version.h.in	2018-06-13 00:50:09.000000000 +0200
+++ patched/src/hb-version.h.in	2018-08-13 02:15:23.534380748 +0200
@@ -45,6 +45,10 @@
 #define HB_VERSION_ATLEAST(major,minor,micro) \
 	((major)*10000+(minor)*100+(micro) <= \
 	 HB_VERSION_MAJOR*10000+HB_VERSION_MINOR*100+HB_VERSION_MICRO)
+#define HB_VERSION_CHECK(major,minor,micro) \
+	((major)*10000+(minor)*100+(micro) >= \
+	 HB_VERSION_MAJOR*10000+HB_VERSION_MINOR*100+HB_VERSION_MICRO)
+
 
 
 HB_EXTERN void
@@ -60,6 +64,11 @@
 		    unsigned int minor,
 		    unsigned int micro);
 
+hb_bool_t
+hb_version_check (unsigned int major,
+		    unsigned int minor,
+		    unsigned int micro);
+
 
 HB_END_DECLS
 
