diff -Naur orig/makefile patched/makefile
--- orig/makefile	2019-06-09 20:09:50.000000000 +0200
+++ patched/makefile	2019-06-17 03:41:19.112907639 +0200
@@ -1,6 +1,8 @@
 LIBS=
 
-PREFIX= /usr/local
+PREFIX= /usr
+
+DESTDIR=
 
 OSNAME := $(shell uname -s)
 
@@ -29,11 +31,11 @@
 
 install:
 	$(MAKE) default
-	-mkdir -p $(PREFIX)/share/
-	-mkdir -p $(PREFIX)/share/applications
-	-mkdir -p $(PREFIX)/bin
-	-mkdir -p $(PREFIX)/share/icons/hicolor/512x512/apps
-	install -m 0644 data/evisum.png $(PREFIX)/share/icons/hicolor/512x512/apps
-	install -m 0644 data/evisum.desktop $(PREFIX)/share/applications
-	install -m 0755 evisum $(PREFIX)/bin
-	install -m 0755 src/tingle/tingle $(PREFIX)/bin
+	-mkdir -p $(DESTDIR)$(PREFIX)/share/
+	-mkdir -p $(DESTDIR)$(PREFIX)/share/applications
+	-mkdir -p $(DESTDIR)$(PREFIX)/bin
+	-mkdir -p $(DESTDIR)$(PREFIX)/share/icons/hicolor/512x512/apps
+	install -m 0644 data/evisum.png $(DESTDIR)$(PREFIX)/share/icons/hicolor/512x512/apps
+	install -m 0644 data/evisum.desktop $(DESTDIR)$(PREFIX)/share/applications
+	install -m 0755 evisum $(DESTDIR)$(PREFIX)/bin
+	install -m 0755 src/tingle/tingle $(DESTDIR)$(PREFIX)/bin
