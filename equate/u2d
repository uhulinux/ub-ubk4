lista=$(curl -k -s https://git.enlightenment.org/apps/equate.git/plain/configure.ac #| grep -E 'm4_define\(\[v' | sed 's/.* \[//g' | sed 's/\].*//g')
#echo $lista | sed 's/ /\./g'
noupdate fejlesztése leállt
