main=$(urllist https://ftp.osuosl.org/pub/rpm/releases/ | grep 'rpm-' | sort -V | tail -n 1)
u2d https://ftp.osuosl.org/pub/rpm/releases/$main/
noupdate Az újabb verzók a le nem tiltható doksi készítéshez újabb haskell progik tömegét igényelné.
