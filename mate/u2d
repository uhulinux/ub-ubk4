urllist https://pub.mate-desktop.org/releases/ | grep -E '^[0-9.]+$' | sort -V | tail -n 1
# urllist https://pub.mate-desktop.org/releases/ | grep -E '^[0-9]+\.[0-9]*['02468'](\.[0-9.])?$' | sort -V | tail -n 1
