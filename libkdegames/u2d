dir=$(urllist https://download.kde.org/stable/applications/ | grep -E '^([0-9])' | sort -V | tail -n 1)
urllist https://download.kde.org/stable/applications/$dir/src/ | splitver '^libkdegames-([0-9.]+).tar.xz$' | sort -V | tail -n 1
